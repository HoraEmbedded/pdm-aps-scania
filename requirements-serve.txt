# Serving dependencies for the container image. No TensorFlow, no notebooks:
# the frozen model is the gradient boosting, and the image is 1.2 GB smaller
# without them.

# Pinned exactly: these three touch model serialisation, and a model trained
# under one version is not guaranteed to load correctly under another.
numpy==1.26.4
scikit-learn==1.5.2
xgboost==3.4.1

# The inference service (src/api.py) and its transport.
# uvicorn is pinned to ensure the HTTP server is stable across builds.
fastapi>=0.110,<1.0
uvicorn[standard]==0.52.4

# The dashboard now calls the API over HTTP (app/streamlit_app.py, tab
# "Via API") instead of only loading the model in-process.
requests>=2.31,<3.0

# Free to move
pandas>=2.0,<3.0
joblib>=1.3
streamlit>=1.36
