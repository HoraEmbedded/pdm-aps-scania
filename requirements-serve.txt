# Pinned exactly: these three touch model serialisation, and a model trained
# with one version is not guaranteed to load correctly under another.
numpy==1.26.4
scikit-learn==1.5.2
xgboost==3.4.1

# Free to move
pandas>=2.0,<3.0
joblib>=1.3
streamlit>=1.36
