# Inference only. TensorFlow is deliberately absent: the frozen model is a
# tree ensemble, and shipping a deep learning framework that inference never
# calls would multiply the image size for nothing.
numpy<2.0
pandas>=2.0,<3.0
scikit-learn>=1.4,<1.6
xgboost>=2.0
joblib>=1.3
streamlit>=1.36
