[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    needs_raw: requires data/raw, fetched by scripts/download_data.sh
    needs_prepared: requires data/processed, written by scripts/build_dataset.py
    needs_model: requires models/, fetched by scripts/fetch_models.sh
    slow: runs a model fit
